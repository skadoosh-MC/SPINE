[pytest]
# @pytest.mark.slow is valid.
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')

# Ignore deprecation warnings from libraries
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning:.*emulation range.*
