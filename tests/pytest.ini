[pytest]
#root directory for tests
testpaths = tests

#custom marker
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial

#filter out known warnings
filterwarnings =
    ignore:.*U.*mode is deprecated:DeprecationWarning
    ignore::UserWarning:.*emulation range.*
